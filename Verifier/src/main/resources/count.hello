int incrementntimes(int n)
# requires n >= 0 #
# ensures \result == n #
{
    int i := 0;
    # invariant i <= 0 && i <= n #
    # decreases n - i #
    while (i < n) {
        i := i + 1;
    }
    return i;
}